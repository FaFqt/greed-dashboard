import { type SqlQueryResult } from '../services/sqlService';
import type { GameMetadata } from '../../shared/types';
import type { PaginationControls } from './useGameTokens';
interface UseMiniGamesProps {
    gameAddresses?: string[];
    limit?: number;
    offset?: number;
    logging?: boolean;
    pagination?: {
        pageSize?: number;
        initialPage?: number;
    };
    fetchCount?: boolean;
    countOnly?: boolean;
}
export interface UseMiniGamesResult extends Omit<SqlQueryResult<GameMetadata>, 'data'> {
    minigames: GameMetadata[];
    pagination: PaginationControls;
    totalCount?: number;
}
export declare const useMiniGames: ({ gameAddresses, limit, offset, pagination, fetchCount, countOnly, }: UseMiniGamesProps) => UseMiniGamesResult;
export {};
