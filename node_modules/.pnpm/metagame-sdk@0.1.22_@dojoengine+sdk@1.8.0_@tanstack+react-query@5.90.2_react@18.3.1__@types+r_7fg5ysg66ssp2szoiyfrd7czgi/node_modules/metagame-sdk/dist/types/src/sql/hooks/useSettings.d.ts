import { type SqlQueryResult } from '../services/sqlService';
import type { GameSettings } from '../../shared/types';
import type { PaginationControls } from './useGameTokens';
interface UseSettingsProps {
    gameAddresses?: string[];
    settingsIds?: number[];
    limit?: number;
    offset?: number;
    logging?: boolean;
    pagination?: {
        pageSize?: number;
        initialPage?: number;
    };
    fetchCount?: boolean;
    countOnly?: boolean;
}
export interface UseSettingsResult extends Omit<SqlQueryResult<GameSettings>, 'data'> {
    settings: GameSettings[];
    pagination: PaginationControls;
    totalCount?: number;
}
export declare const useSettings: ({ gameAddresses, settingsIds, limit, offset, logging, pagination, fetchCount, countOnly, }: UseSettingsProps) => UseSettingsResult;
export {};
