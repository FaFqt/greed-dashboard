export interface SqlQueryResult<T> {
    data: T[];
    loading: boolean;
    error: string | null;
    refetch: () => Promise<void>;
}
export declare function useSqlQuery<T>(toriiUrl: string | undefined, query: string | null | undefined, logging?: boolean): SqlQueryResult<T>;
export declare function executeSqlQuery<T>(toriiUrl: string, query: string | null | undefined, logging?: boolean): Promise<{
    data: T[];
    error: string | null;
}>;
