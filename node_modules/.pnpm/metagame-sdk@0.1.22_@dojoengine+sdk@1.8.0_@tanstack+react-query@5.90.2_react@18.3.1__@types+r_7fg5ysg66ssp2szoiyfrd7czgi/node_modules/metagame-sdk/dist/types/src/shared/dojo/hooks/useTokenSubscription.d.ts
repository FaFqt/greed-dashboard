import { SchemaType } from '@dojoengine/sdk';
import { MetagameClient } from '../../client';
export interface UseTokenSubscriptionOptions {
    contractAddress: string;
    enabled?: boolean;
    logging?: boolean;
    transform?: (entity: any) => any;
    onUpdate?: (entity: any) => void;
}
export interface UseTokenSubscriptionResult<T> {
    entities: T[] | null;
    isSubscribed: boolean;
    error?: Error;
}
export interface SubscriptionResult<T> {
    entities: T[] | null;
    unsubscribe: () => void;
}
/**
 * Base hook for subscribing to entity queries
 */
export declare function useTokenSubscription<S extends SchemaType, T = any>(client: MetagameClient<S> | null, options: UseTokenSubscriptionOptions): UseTokenSubscriptionResult<T>;
