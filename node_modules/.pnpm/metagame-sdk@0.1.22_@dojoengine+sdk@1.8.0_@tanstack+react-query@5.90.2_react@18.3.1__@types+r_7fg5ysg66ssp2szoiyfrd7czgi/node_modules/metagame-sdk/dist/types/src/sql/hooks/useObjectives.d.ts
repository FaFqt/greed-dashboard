import { type SqlQueryResult } from '../services/sqlService';
import type { GameObjective } from '../../shared/types';
import type { PaginationControls } from './useGameTokens';
interface UseGameObjectivesProps {
    gameAddresses?: string[];
    objectiveIds?: number[];
    limit?: number;
    offset?: number;
    logging?: boolean;
    pagination?: {
        pageSize?: number;
        initialPage?: number;
    };
    fetchCount?: boolean;
    countOnly?: boolean;
}
export interface UseObjectivesResult extends Omit<SqlQueryResult<GameObjective>, 'data'> {
    objectives: GameObjective[];
    pagination: PaginationControls;
    totalCount?: number;
}
export declare const useObjectives: ({ gameAddresses, objectiveIds, limit, offset, logging, pagination, fetchCount, countOnly, }: UseGameObjectivesProps) => UseObjectivesResult;
export {};
