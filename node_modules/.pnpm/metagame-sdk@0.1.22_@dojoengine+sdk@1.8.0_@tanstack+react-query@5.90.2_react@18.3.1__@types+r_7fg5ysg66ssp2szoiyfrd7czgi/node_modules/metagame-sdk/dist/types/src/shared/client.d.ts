import { SchemaType } from '@dojoengine/sdk';
import { createDojoStore } from '@dojoengine/sdk/react';
import { MetagameConfig } from './types';
export declare const DEFAULT_DOJO_CONFIG: {
    domain: {
        name: string;
        version: string;
        chainId: string;
        revision: string;
    };
};
export declare class MetagameClient<T extends SchemaType> {
    private config;
    private store;
    private namespace;
    private tokenAddress;
    constructor(config: MetagameConfig<T>);
    /**
     * Create a MetagameClient instance with optional automatic dojoSDK initialization
     */
    static create<T extends SchemaType>(config: MetagameConfig<T>): Promise<MetagameClient<T>>;
    /**
     * Get the configuration
     */
    getConfig(): MetagameConfig<T>;
    /**
     * Get the store instance
     */
    getStore(): ReturnType<typeof createDojoStore<T>>;
    /**
     * Get the namespace
     */
    getNamespace(): string;
    /**
     * Get the token address
     */
    getTokenAddress(): string;
    /**
     * Get the Torii URL
     */
    getToriiUrl(): string;
}
