import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext, useState, useEffect } from 'react';
const MetagameContext = createContext(null);
export const useMetagame = () => {
    const context = useContext(MetagameContext);
    if (!context) {
        throw new Error('useMetagame must be used within a MetagameProvider');
    }
    return context;
};
export const MetagameProvider = ({ metagameClient, children, }) => {
    const [client, setClient] = useState(null);
    useEffect(() => {
        setClient(metagameClient);
    }, [metagameClient]);
    if (!client) {
        return _jsx("div", { children: "Loading Metagame client..." });
    }
    return _jsx(MetagameContext.Provider, { value: client, children: children });
};
